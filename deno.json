{
  "desktop": {
    "app": {
      "name": "Facet",
      "identifier": "net.ytakahashi.facet",
      "icons": {
        "macos": "./public/icon.icns"
      }
    },
    "backend": "webview"
  },
  "imports": {
    "@std/http/file-server": "jsr:@std/http@^1/file-server"
  },
  "tasks": {
    "desktop": "deno task build && deno desktop --allow-read --allow-write --allow-env --allow-net --exclude node_modules --include dist main.ts",
    "desktop:hmr": "deno task build && deno desktop --hmr --allow-read --allow-write --allow-env --allow-net --exclude node_modules --include dist main.ts"
  }
}
